import java.io.InputStream;


/**
 * This class is just a wrapper holding the Stream (if connection accepted) and response code from the connection.
 * 
 * @author Paul Day
 *
 */
public class StreamHolder {
		int response;
		InputStream stream;
}
